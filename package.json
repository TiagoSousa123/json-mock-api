{
	"name": "json-mock-api",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "json-server --watch src/db.json --port 8000",
		"generate": "json-server src/index.js --port 9000",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"json-schema-faker": "^0.5.0-rcv.33",
		"json-server": "^0.16.3"
	}
}
